// Fill these public values after creating the Supabase project.
// Keeping both empty enables the browser-only demo mode.
window.PORRA_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
  // Emails that should be treated as site administrators.
  adminEmails: ["admin@admin.admin"],
};
